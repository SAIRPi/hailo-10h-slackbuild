###############################################################################
# SAIRPi Project - Slackware AI on Raspberry Pi
# https://sairpi.penthux.net
#
# HAILO BUILD TO-DO DOCUMENTATION
#
# Platform : Raspberry Pi 5 (AArch64 / armv8)
# OS       : Slackware AArch64 -current
# Hardware : Hailo-10H M.2 AI Accelerator Module
# Linux    : kernel 6.18.26
# HailoRT  : v5.3.0
#
# This document records outstanding tasks, issues and items requiring
# attention during the SAIRPi/HAILO build development process.
#
# Maintainer: sairpi.project@penthux.net
# Updated   : 2026-05-11
###############################################################################


# TODO 001 - CHROOT DUPLICATE kernel-modules-rpi5 PACKAGE ENTRIES

Status  : DONE 2026-05-06
Priority: MEDIUM

Description:
  The chroot at /tmp/sairpi-chroot has duplicate kernel-modules-rpi5 package
  entries in /tmp/sairpi-chroot/var/log/packages/:
    kernel-modules-rpi5-6.18.26-armv8-1_slackcurrent_02May26_sai
    kernel-modules-rpi5-6.18.26-armv8-1_slackcurrent_05May26_sai

  These are a result of unauthorised package installations made in a previous
  session. The chroot state is inconsistent and needs to be cleaned up before
  the next clean build cycle.

Action required:
  Remove the duplicate 02May26 entry from /tmp/sairpi-chroot/var/log/packages/
  and verify the chroot package state is consistent before next build.

------

# TODO 002 - INDIVIDUAL BUILD DIRECTORIES FROM MAIN SCRIPTS

Status  : DONE 2026-05-09
Priority: HIGH

Description:
  Build scripts have doinst.sh and slack-desc content hard-coded into them. 
  This was for ease of development but before the build scripts are released 
  publicly they should be individual files

Action required:
  Move all package build scripts into individual build directories so they
  are separate and can be managed easier.
  
------

# TODO 003 - LOGGING

Status  : DONE 2026-05-10
Priority: HIGH

Description:
  Logging is a mess. Convoluted and out of order with the build process.
  Cumbersome and time consuming to follow and troubleshoot. 

Action required:
  Remove multiple headers and consolidate them in a function globally. 
  Start logging with stage 1 - copy to stage 2 - append and combine for
  final build log. Then move to a build logs directory for posterity.
  
  * chroot pkg forge and cmake build will still contain their own headers.
  
  Just keep them separate.
  
  * That's already the plan! chroot pkg forge isn't worth incorporating.
  cmake might be but it's a legacy install so we'll keep it that way.
  
------

# TODO 004 - MOVE BSP DOWNLOAD DIR

Status  : DONE 2026-05-11
Priority: MEDIUM

Description:
  SARPi board support packages are downloaded to build pwd.

Action required:
  Move SARPi BSP downloads to /tmp/SBo directory.

------

# TODO 005 - CONTROL SCRIPT

Status  : REJECTED 2026-05-08
Priority: N/A

Description:
  Control script to manage/update/remove sources the same as sarpi builds.

Action required:
  Needs discussing and creating.
  
  * Hailo-10H SlackBuild is not same user-base target as SARPi. If Hailo
  didn't lock RT build to cmake 3.3, a control script would be an excellent 
  idea.

------

# EOF<*>
